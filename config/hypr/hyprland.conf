monitor=eDP-1,1920x1080@60,0x0,1


exec-once = ~/.autostart
exec=libinput-gestures-setup restart &
exec-once = hyprpaper
exec-once = eww open bar

# source = ~/.config/hypr/colors.conf

input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    touchpad {
        natural_scroll = yes
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    gaps_in = 5
    gaps_out = 20
    border_size = 2
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    layout = dwindle
}

decoration {
    rounding = 10
    blur = yes
    blur_size = 3
    blur_passes = 3
    blur_new_optimizations = on

    drop_shadow = yes
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)
}

animations {
    enabled = yes

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    new_is_master = false
}

gestures {
    workspace_swipe = on
    workspace_swipe_invert=true
}

device:epic mouse V1 {
    sensitivity = -0.5
}


# ------------------- keybindings ------------------- #
$mainMod = SUPER

# window manipulation
bind = $mainMod SHIFT, Q, exit,
bind = $mainMod SHIFT, R, exec, hyprctl reload
bind = $mainMod, Q, killactive,
bind = $mainMod, C, pseudo,
bind = $mainMod, F, fullscreen,

# apps keybindings
bind = $mainMod, B, exec, firefox
bind=$mainMod, Return, exec, kitty

# bind=ALT,B,exec, pkill -SIGUSR1 '^waybar$'

# bind=,Print,exec,grim $HOME/Pictures/SS/$(date +'%Y%m%d%H%M%S_1.png') && notify-send 'Screenshot Saved'
# bind=SUPER,Print,exec,grim - | wl-copy && notify-send 'Screenshot Copied to Clipboard'
# bind=SUPERSHIFT,Print,exec,grim - | swappy -f -

# bind=ALT,D,exec,~/.config/rofi/scripts/launcher_t1
# bind=ALT,T,exec,~/.local/bin/theme_changer_WL
# bind=ALTCONTROL,X,exec,~/.local/bin/powermenu
# bind=ALTCONTROL,K,exec,~/.local/bin/hypr_bindings

# bind=ALTCONTROL,E,exec,gammastep -P -O 5000
# bind=ALTCONTROL,R,exec,gammastep -x

# bind=ALTSHIFT,P,exec, gtklock

bind = $mainMod SHIFT, T, exec, swaync-client -t

bind = $mainMod, M, exit, 
# bind = $mainMod, E, exec, dolphin
# bind = $mainMod, R, exec, wofi --show drun
bind = $mainMod, P, pseudo, # dwindle

# Move focus with mainMod + vim keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# Switch workspaces
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

bind = $mainMod, tab, workspace, +1
bind = $mainMod SHIFT, tab, workspace, -1

# Move windows in workspace
bind = $mainMod SHIFT, h, movewindow, l
bind = $mainMod SHIFT, l, movewindow, r
bind = $mainMod SHIFT, k, movewindow, u
bind = $mainMod SHIFT, j, movewindow, d

# Resize in workspace
bind = $mainMod CONTROL, h, splitratio, -0.1
bind = $mainMod CONTROL, l, splitratio, +0.1

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Layout
bind = $mainMod CTRL, Space, togglefloating
bind = $mainMod, Space, togglesplit


bind = $mainMod SHIFT, V, togglegroup
bind = $mainMod, N, changegroupactive, f
bind = $mainMod SHIFT, N, changegroupactive, b

# Laptop keys
bind =,XF86MonBrightnessUp, exec, ~/.local/bin/brightness up
bind =,XF86MonBrightnessDown, exec, ~/.local/bin/brightness down
bind =,XF86AudioPlay, exec, playerctl play-pause
bind =,XF86AudioRaiseVolume, exec, ~/.local/bin/volume up
bind =,XF86AudioLowerVolume, exec, ~/.local/bin/volume down
bind =,XF86AudioMute, exec, ~/.local/bin/volume mute

# misc
bind = $mainMod, W, exec, ~/.local/bin/wallpaper

# ---------------------- rules ---------------------- #
# Float Necessary Windows
windowrule = float,transmission
windowrule = float,eog

# windowrule=float,Rofi
# windowrule=float,pavucontrol
# windowrulev2 = float,class:^()$,title:^(Picture in picture)$
# windowrulev2 = float,class:^(brave)$,title:^(Save File)$
# windowrulev2 = float,class:^(brave)$,title:^(Open File)$
# windowrulev2 = float,class:^(LibreWolf)$,title:^(Picture-in-Picture)$
# windowrulev2 = float,class:^(blueman-manager)$
# windowrulev2 = float,class:^(org.twosheds.iwgtk)$
# windowrulev2 = float,class:^(blueberry.py)$
# windowrulev2 = float,class:^(xdg-desktop-portal-gtk)$
