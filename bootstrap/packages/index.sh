#!/usr/bin/env bash

. "$(dirname "$0")/packages.sh"

# . "$(dirname "$0")/i3lock-color.sh"
# . "$(dirname "$0")/imagemagick.sh"
. "$(dirname "$0")/betterlockscreen.sh"

. "$(dirname "$0")/commitizen.sh"

. "$(dirname "$0")/deno.sh"
. "$(dirname "$0")/golang.sh"
. "$(dirname "$0")/node.sh"

. "$(dirname "$0")/fish.sh"
. "$(dirname "$0")/starship.sh"

. "$(dirname "$0")/awesomewm.sh"
. "$(dirname "$0")/picom.sh"

. "$(dirname "$0")/lf.sh"

. "$(dirname "$0")/gnome.sh"
. "$(dirname "$0")/ulauncher.sh"

. "$(dirname "$0")/inkscape.sh"

. "$(dirname "$0")/tpm.sh"

. "$(dirname "$0")/zk.sh"
